# --- REQUIRED ---
PACT_BROKER_BASE_URL=https://<your-subdomain>.pactflow.io
PACT_BROKER_TOKEN=<your-token>
BRANCH=main

# --- OPTIONAL (defaults shown) ---
# PORT=9001
# PROVIDER_BASE_URL=http://127.0.0.1:9001

# Stamp verification results with a real version (CI build, Git SHA, etc.)
PROVIDER_VERSION=1.2.3+build.456

# Corporate TLS (preferred)
SSL_CERT_FILE=C:\Users\<you>\.zscaler-ca-bundle.pem
# or: NODE_EXTRA_CA_CERTS=C:\path\to\corp-root-ca.pem

# Last resort (avoid if possible; only for temporary debugging)
# PACT_DISABLE_SSL_VERIFICATION=true
